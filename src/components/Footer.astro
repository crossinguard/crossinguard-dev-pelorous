---
const today = new Date();
---

<footer>
    <p>W. Brett Egbert &copy; {today.getFullYear()}</p>
    <ul class="social cluster">
        <li><a href="mailto:brett@crossinguard.dev">email</a></li>
        <li>
            <a href="https://github.com/crossinguard" target="_blank">github</a>
        </li>
        <li>
            <a
                href="https://www.linkedin.com/in/w-brett-egbert/"
                target="_blank">linkedin</a
            >
        </li>
    </ul>
    <p><em>stay curious</em></p>
</footer>

<style>
    footer {
        padding-block: 2rem;
        background: var(--color-primary-shade);
        color: var(--color-light);
        font-family: monospace;
        font-size: var(--font-s);
        box-shadow:
            0 -0.25rem var(--color-primary),
            0 -0.5rem var(--color-secondary),
            0 -0.75rem var(--color-tertiary);
        text-align: center;
        margin-block-start: 2rem;
    }

    p {
        margin-inline: auto;
    }

    .social {
        --cluster-horizontal-alignment: center;
        padding-inline: 0;
    }

    .social li {
        list-style: none;
    }

    .social a {
        text-decoration: none;
        font-variation-settings: "wght" 600;
        text-transform: uppercase;
    }

    .social a:hover {
        color: var(--color-tertiary);
        text-decoration: underline;
        text-decoration-color: var(--color-primary);
        text-decoration-thickness: 0.25rem;
        text-underline-offset: 0.25rem;
    }

    .social a::before {
        content: "[ ";
    }

    .social a::after {
        content: " ]";
    }
</style>
